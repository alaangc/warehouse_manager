{
  "name": "warehouse-manager",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace @warehouse-manager/web",
    "build": "npm run build --workspace @warehouse-manager/web",
    "lint": "npm run lint --workspace @warehouse-manager/web",
    "typecheck": "npm run typecheck --workspace @warehouse-manager/web"
  },
  "engines": { "node": ">=24" }
}
